// All sounds have been removed as per user request.

/** Plays the standard click sound. (No-op) */
export const playClick = () => {};

/** Plays the success sound effect. (No-op) */
export const playSuccess = () => {};

/** Plays the delete sound effect. (No-op) */
export const playDelete = () => {};